---
import { getIndex } from "@lib/contentParser";
import EntryLayout from "@components/index-cards/EntryLayout.astro";
import type { IndexCardsEntry } from "@/types";

const entry = (await getIndex("index-cards")) as IndexCardsEntry;
---

<EntryLayout entry={entry}/>
